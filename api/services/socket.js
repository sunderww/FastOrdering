module.exports = {

/*    var _SOCKET = function(){
	var sockets = [];

	function addSocket(key, socketId) {
	    this.sockets.push;
	}

	function getIdSocket(key) {
	    this.sockets.forEach(entry) {
		if (entry.key == key)
		    return entry.socketId;
	    }
	}
    };
*/
}